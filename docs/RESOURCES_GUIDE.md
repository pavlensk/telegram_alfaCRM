# 🏗️ Sports Bot - Структура проекта

## Структура папок

```
project/
├── main.py                    # Главное приложение (рефакторено с ресурсами)
├── .env                       # Переменные окружения
├── requirements.txt           # Зависимости
├── resources/                 # 📁 Папка с ресурсами
│   ├── ui_labels.json        # Метки кнопок и элементов UI
│   ├── sections.json         # Конфиг секций (плавание, бег, триатлон)
│   ├── quiz_questions.json   # Вопросы и результаты квиза
│   └── texts.json            # Текстовые сообщения боту
└── README.md
```

## 📋 Описание ресурсных файлов

### 1. **ui_labels.json** - Метки кнопок
Содержит все текстовые метки для кнопок в интерфейсе.

**Когда менять:** Когда нужно изменить текст кнопок.

```json
{
  "btn_swimming": "💙 Плавание",
  "btn_running": "💚 Бег",
  ...
}
```

### 2. **sections.json** - Секции навигации
Определяет доступные направления (Плавание, Бег, Триатлон) и приветственные сообщения.

**Когда менять:** Когда меняется название секции или сообщение при выборе.

```json
{
  "root_title": "Выберите направление:",
  "sections": {
    "swimming": {
      "title": "Плавание",
      "hello": "💙 Привет! Вопрос по направлению Плавание."
    },
    ...
  }
}
```

### 3. **quiz_questions.json** - Квиз и результаты
Все вопросы квиза, варианты ответов, scores и результаты уровней.

**Когда менять:** 
- При изменении вопросов квиза
- При добавлении новых уровней
- При изменении описания уровней

```json
{
  "quiz_ttl_seconds": 3600,
  "quiz_indices": { ... },
  "questions": [ ... ],
  "level_results": { ... }
}
```

### 4. **texts.json** - Текстовые сообщения
Все текстовые сообщения, выводимые пользователю.

**Когда менять:** Когда нужно отредактировать или переводить текст.

```json
{
  "sw_cert": "...",
  "sw_take": "...",
  "quiz_expired": "...",
  ...
}
```

## 🔄 Как использовать в коде

### Загрузка ресурсов
```python
from resources_loader import Resources

ui_labels = Resources.load("ui_labels.json")
```

### Обращение к значениям
```python
# Из main.py:
button_text = UI_LABELS["btn_swimming"]
section_title = SECTION_TITLES[Section.SWIMMING]
question = SWIMMING_LEVEL_QUESTIONS[0]
message = TEXTS["quiz_expired"]
```

## ⚙️ Интеграция с main.py

### В начале файла:
```python
from pathlib import Path
import json

class Resources:
    RESOURCES_DIR = Path(__file__).parent / "resources"
    
    @classmethod
    def load(cls, filename: str) -> Dict:
        path = cls.RESOURCES_DIR / filename
        with open(path, "r", encoding="utf-8") as f:
            return json.load(f)

# Загружаем все ресурсы при старте
UI_LABELS = Resources.load("ui_labels.json")
SECTIONS = Resources.load("sections.json")
QUIZ_DATA = Resources.load("quiz_questions.json")
TEXTS = Resources.load("texts.json")
```

## 📝 Пример: Добавить новый уровень

1. Отредактируй `resources/quiz_questions.json`:
```json
"(9,10)": {
  "title": "🏅 Pro",
  "desc": "Для профессионалов...",
  "path": "/pro-level"
}
```

2. Все готово! Ничего менять в main.py не нужно.

## 🚨 Валидация ресурсов

Если JSON невалиден, бот выведет ошибку:
```
❌ Failed to load resources: Invalid JSON in quiz_questions.json: ...
```

Проверьте синтаксис JSON в редакторе перед сохранением.

## 💡 Преимущества

| До | После |
|----|-------|
| 500 строк констант | 4 JSON файла |
| Баги от опечаток | Быстрая валидация |
| Нужен разработчик | Может редактировать любой |
| Передеплой на каждое изменение | Git commit текста |
| Сложно локализовать | Легко добавить новый язык |

## 📱 Структура на Render/Heroku

```bash
# При деплое убедись, что папка resources/ включена в git:
git add resources/
git commit -m "Update quiz questions"
git push
```

## 🔐 Безопасность

- ✅ Ресурсы не содержат секреты (используй .env для них)
- ✅ JSON валидируется при старте
- ✅ Ошибки обрабатываются gracefully
- ✅ Все путями relative, работают везде

---

**Готово к продакшену!** 🚀
